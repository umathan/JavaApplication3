package Chapter5Practice;

public class PreventException {

public static void main (String [] args){
   
}
}
/*
    String choice = "y";
while(choice.equalsIgnoreCase("Y")){
    double subtotal = 0.0;
            System.out.println("Enter subtotal ");
            if (sc.hasNextDouble()){
                subtotal = sc.nextDouble();
                sc.nextLine();
            }else{
                System.out.println("Error! Invalid number. try Again. \n");
                sc.nextLine();
                continue; 
            }
}

if (! customerType = null){
    if(customerType.equals(r)){
        discountPercent = .
    }
}

Scanner sc = new Scanner(System.in);
double subtotal = 0.0;
boolean isValid = false;

while (!isValid){
            System.out.println("Enter subtotal");
            
            if(sc.hasNextDouble()){
                subtotal = sc.nextDouble();
                isValid = true;
            }
            else {
                System.out.println("Error! invalid number. please try again");
            }
            sc.nextLine();
            
            // check the range of the double value
            
            if (isValid && subtotal <= 0){
                System.out.println("Error! Number must be greater than 0");
                isValid = false;
            }else if (isValid && subtotal >=1000){
                System.out.println("Error. Number must be less than 1000");
                isValid = false;
            }
          
}


  */